# ── Rabbit Hole deploy config ──
# Copy this to .deploy.env and fill in your values.

# SSH host string (user@hostname-or-ip)
DEPLOY_HOST="user@rh.grabenstein.cloud"

# Directory on the remote server where the image will be staged
DEPLOY_PATH="/opt/rabbit-hole"
